{
  "name": "portfolio-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "config": "tsx ../scripts/generate-wrangler.ts",
    "dev": "npm run config && wrangler dev --config wrangler.generated.json",
    "build": "npm run config && wrangler deploy --dry-run --outdir=dist --config wrangler.generated.json",
    "deploy": "npm run config && wrangler deploy --config wrangler.generated.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  }
}
